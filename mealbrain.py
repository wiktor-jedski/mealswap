import pandas


class MealBrain:
    """Manages products and meals."""

    def add_product(self, name, unit, fat, carb, protein, hidden):
        """Adds a product to the database."""

    def add_meal(self, name, unit, **kwargs):
        """Adds a meal to the database."""

    def change(self, key, **kwargs):
        """Changes a product in the database."""

    def delete(self, key):
        """Deletes a product from the database."""

